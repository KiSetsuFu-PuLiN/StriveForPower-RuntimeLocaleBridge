运行时文本翻译桥。

扫描可见界面文本，先走 Godot/本地 JSON 缓存，缓存缺失时使用 DeepSeek Chat Completions 翻译。

配置位置：RuntimeLocaleBridge/settings.json
可在游戏 Options -> Settings 中填写 DeepSeek API Key。已翻译文本会写入 cache/translations.json，后续不会重复请求。
